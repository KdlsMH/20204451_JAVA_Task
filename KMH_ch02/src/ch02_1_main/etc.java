package ch02_1_main;

public class etc {
	/*
	// ','를 사용하여 변수를 한번에 선언
	int x;
	int y;
	int z;	//이것과	int x, y, z; 은 같다.
	 
	//값저장
	int score; //변수 선언
	score = 90; //값 저장

	//잘못된 코딩 예
	int value;	//변수  value 선언
	int result = value + 10;	//변수 value 값을 읽고 10을 더해져 변수 result에 저장
	
	//올바른 코딩 예
	int value = 30;	//변수 value가 30으로 초기화됨
	int result value + 10;	//변수 value 값(30)을 읽고 10을 더해서 변수 result 저장

	//변수사용

 	int hour = 3;
 	int minute = 5;
println()을 사용하여 출련함
 	System.out.println(hour + "시간" + minute + "분"); 
 	//변수 hour와 minute 값을 출력: 3시간 5분
 산술 연산식에 변수를 사용하여 저장된값으로 연산을 수행
	int totalMinute = (hour * 60) + minute;
	int totalMinute = (3 * 60) + minute;
	int totalMinute = 180 + 5;
	int totalMinute = 185;
변수는 또 다른 변수에 대입하여 값을 복사할 수 있음
int x = 10; //변수 xdp 10을 저장
int y = x; //x에 저장된 변수 y 에 복사(저장) 
	 
	 */
}
